# actionbuild.yml
name: 'actionbuild'
description: 'build app'

#outputs: #output des fichiers???
#  apkfile: # id of output
#    description: 'apk de l appli'

runs:
  using: 'docker'
  image: 'Dockerfile'